import { GameRoomPipePipe } from './game-room-pipe.pipe';

describe('GameRoomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GameRoomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
