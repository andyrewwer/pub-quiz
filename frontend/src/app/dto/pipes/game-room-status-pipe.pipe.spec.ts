import { GameRoomStatusPipePipe } from './game-room-status-pipe.pipe';

describe('GameRoomStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GameRoomStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
