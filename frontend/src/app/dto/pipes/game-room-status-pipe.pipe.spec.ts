import { GameRoomStatusPipe } from './game-room-status-pipe.pipe';

describe('GameRoomStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GameRoomStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
